[FilterPlugin]
; Additional files to include when packaging the plugin.
; Standard plugin folders such as /Config, /Content, /Resources and /Source are already handled by UE.
; Only list extra root files or optional folders here.


; Optional extra helper files / samples that are safe to distribute.

